# DDC Biotech Intranet - Быстрый старт

## Проект готов к использованию!

### Доступные учётные записи

1. **admin** / admin123
   - Роль: Системный администратор
   - Полный доступ ко всем функциям
   - Доступ к админ-панели

2. **labhead** / labhead123
   - Роль: Заведующий лабораторией
   - Может создавать и редактировать реагенты, рецепты
   - Доступ к админ-панели

3. **employee** / employee123
   - Роль: Сотрудник
   - Просмотр и базовые операции
   - Нет доступа к админ-панели

### Запуск проекта

1. Активируйте виртуальное окружение:
```bash
# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

2. Запустите сервер разработки:
```bash
python manage.py runserver
```

3. Откройте в браузере:
   - **Главная страница**: http://127.0.0.1:8000/
   - **Админ-панель**: http://127.0.0.1:8000/admin/

### Структура проекта

```
ddc_project_lite/
├── ddc_intranet/          # Конфигурация проекта
├── intranet/              # Основное приложение
│   ├── models.py          # Все модели (User, Reagent, Task, Culture, etc.)
│   ├── views.py           # Все представления (FBV)
│   ├── forms.py           # Все формы
│   ├── admin.py           # Настройка админки
│   ├── urls.py            # URL-маршруты
│   └── templatetags/      # Шаблонные теги
├── templates/             # HTML-шаблоны
├── static/                # Статические файлы (CSS, JS)
├── media/                 # Загруженные файлы
└── db.sqlite3             # База данных SQLite
```

### Основные функции

#### Дашборд (Главная страница)
- ✓ 3 виджета с данными из разных таблиц
- ✓ Статистические карточки
- ✓ Поиск по всей системе
- ✓ Пагинация

#### CRUD Реагентов
- ✓ Список реагентов с фильтрацией
- ✓ Детальная страница реагента
- ✓ Создание, редактирование, удаление
- ✓ Контроль доступа по ролям
- ✓ История движений реагентов

#### Другие функции
- ✓ Управление задачами и комментариями
- ✓ Культуры клеток с событиями
- ✓ Рецептуры с составом
- ✓ Объявления
- ✓ Календарь событий
- ✓ Загрузка документов

### Демонстрация возможностей

#### ORM и запросы
- filter(), exclude(), order_by()
- values(), values_list(), count(), exists()
- aggregate(), F(), Q()
- select_related(), prefetch_related()
- Кастомные менеджеры

#### Формы
- ModelForm с различными виджетами
- Валидация через clean_<field>()
- Переопределённый save()
- Обработка загрузки файлов
- class Media для JS/CSS

#### Шаблоны
- Наследование (extends, include)
- Кастомные теги (@simple_tag, @inclusion_tag)
- Кастомные фильтры
- Отображение ошибок форм

#### Админ-панель
- list_display, list_filter, search_fields
- Inline-модели
- raw_id_fields, filter_horizontal
- Кастомные display-методы
- Admin actions с генерацией PDF

### Тестовые данные

В базе данных созданы:
- 3 пользователя с разными ролями
- 6 реагентов (некоторые критические)
- 2 культуры клеток
- 3 задачи с разными статусами
- 3 объявления (2 закреплённых)
- 1 рецептура
- 1 событие календаря

### Технологии

- Django 4.2.16
- SQLite (с возможностью переключения на PostgreSQL)
- Bootstrap 5
- Pillow (обработка изображений)
- ReportLab (генерация PDF)
- django-debug-toolbar

### Полезные команды

```bash
# Создать миграции
python manage.py makemigrations

# Применить миграции
python manage.py migrate

# Создать суперпользователя
python manage.py createsuperuser

# Запустить сервер
python manage.py runserver

# Открыть Django shell
python manage.py shell

# Собрать статику (для production)
python manage.py collectstatic

# Создать тестовые данные заново
python create_test_data.py
```

### Переключение на PostgreSQL

1. Раскомментируйте в requirements.txt:
```python
psycopg2-binary==2.9.9
```

2. Установите:
```bash
pip install psycopg2-binary
```

3. В settings.py раскомментируйте и настройте:
```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'ddc_intranet',
        'USER': 'postgres',
        'PASSWORD': 'your_password',
        'HOST': 'localhost',
        'PORT': '5432',
    }
}
```

4. Примените миграции к новой базе:
```bash
python manage.py migrate
python create_test_data.py
```

### Особенности реализации

#### Студенческий монолитный подход
- Всё в одном приложении intranet
- Все модели в одном models.py
- Все формы в одном forms.py
- Все представления в одном views.py (FBV)
- Простая и понятная структура

#### Демонстрация всех требований
✓ Задание 15: Главная страница с виджетами и поиском
✓ Задание 16: Детальная страница объекта с полным CRUD
✓ Все возможности ORM
✓ Все возможности форм
✓ Кастомизация админки
✓ Шаблонные теги и фильтры
✓ Кеширование
✓ Интернационализация (русский язык)
✓ Debug toolbar
✓ Обработка файлов

### Контакты и поддержка

Проект создан для демонстрации навыков работы с Django в рамках курсовой работы.

Enjoy coding! 🚀


