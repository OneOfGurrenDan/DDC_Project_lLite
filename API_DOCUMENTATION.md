# DDC Biotech API Документация

REST API для интранет-портала DDC Biotech.

## Базовый URL

```
http://127.0.0.1:8000/api/
```

## Аутентификация

API требует аутентификации для всех запросов. Поддерживаются следующие методы:
- **Session Authentication** - используйте после входа через веб-интерфейс
- **Basic Authentication** - отправляйте username и password в заголовках

### Вход через браузер
```
http://127.0.0.1:8000/api-auth/login/
```

## Browsable API

Django REST Framework предоставляет веб-интерфейс для тестирования API:
```
http://127.0.0.1:8000/api/
```

## Доступные Endpoints

### 📊 Главный корень API
```
GET http://127.0.0.1:8000/api/
```
Возвращает список всех доступных endpoints.

---

## 👥 Пользователи

### Список пользователей
```
GET http://127.0.0.1:8000/api/users/
```

**Параметры запроса:**
- `search` - поиск по username, имени, фамилии, email
- `ordering` - сортировка по полям (username, date_joined)
- `page` - номер страницы (пагинация)

**Пример:**
```
GET http://127.0.0.1:8000/api/users/?search=admin&ordering=username
```

### Детальная информация о пользователе
```
GET http://127.0.0.1:8000/api/users/{id}/
```

### Текущий пользователь
```
GET http://127.0.0.1:8000/api/users/me/
```
Возвращает информацию о текущем авторизованном пользователе.

---

## 🧪 Реагенты

### Список реагентов
```
GET http://127.0.0.1:8000/api/reagents/
```

**Параметры запроса:**
- `search` - поиск по названию и категории
- `category` - фильтр по категории (buffer, enzyme, antibody, chemical, media, other)
- `is_critical` - показать только реагенты с критичным остатком (true)
- `active_only` - показать только активные реагенты (true)
- `ordering` - сортировка (name, on_hand, expiry_date, created_at)
- `page` - номер страницы

**Примеры:**
```
GET http://127.0.0.1:8000/api/reagents/?category=enzyme
GET http://127.0.0.1:8000/api/reagents/?is_critical=true
GET http://127.0.0.1:8000/api/reagents/?active_only=true&ordering=-on_hand
```

### Создать реагент
```
POST http://127.0.0.1:8000/api/reagents/
Content-Type: application/json

{
    "name": "Tris-HCl",
    "category": "buffer",
    "on_hand": 500.0,
    "min_threshold": 100.0,
    "expiry_date": "2025-12-31"
}
```

### Детальная информация о реагенте
```
GET http://127.0.0.1:8000/api/reagents/{id}/
```

### Обновить реагент
```
PUT/PATCH http://127.0.0.1:8000/api/reagents/{id}/
```

### Удалить реагент
```
DELETE http://127.0.0.1:8000/api/reagents/{id}/
```

### Критичные реагенты
```
GET http://127.0.0.1:8000/api/reagents/critical/
```
Возвращает список реагентов, у которых остаток меньше или равен минимальному порогу.

### Реагенты с истекающим сроком годности
```
GET http://127.0.0.1:8000/api/reagents/expiring/
```
Возвращает реагенты, срок годности которых истекает в течение 30 дней.

### Движения реагента
```
GET http://127.0.0.1:8000/api/reagents/{id}/movements/
```
Возвращает историю движений (приход/расход) конкретного реагента.

---

## 📦 Движения реагентов

### Список движений
```
GET http://127.0.0.1:8000/api/reagent-movements/
```

**Параметры запроса:**
- `search` - поиск по названию реагента и комментарию
- `ordering` - сортировка (date, quantity)
- `page` - номер страницы

### Создать движение
```
POST http://127.0.0.1:8000/api/reagent-movements/
Content-Type: application/json

{
    "reagent": 1,
    "quantity": 50.0,
    "movement_type": "in",
    "comment": "Получено от поставщика"
}
```
**Примечание:** Поле `user` устанавливается автоматически (текущий пользователь).

### Детальная информация о движении
```
GET http://127.0.0.1:8000/api/reagent-movements/{id}/
```

---

## 📋 Рецептуры

### Список рецептур
```
GET http://127.0.0.1:8000/api/recipes/
```

**Параметры запроса:**
- `search` - поиск по названию и описанию
- `status` - фильтр по статусу (draft, approved, archived)
- `ordering` - сортировка (name, created_at, status)
- `page` - номер страницы

**Пример:**
```
GET http://127.0.0.1:8000/api/recipes/?status=approved
```

### Создать рецептуру
```
POST http://127.0.0.1:8000/api/recipes/
Content-Type: application/json

{
    "name": "LB Medium",
    "description": "Luria-Bertani среда для бактерий",
    "status": "draft"
}
```

### Детальная информация о рецептуре
```
GET http://127.0.0.1:8000/api/recipes/{id}/
```

### Утвердить рецептуру
```
POST http://127.0.0.1:8000/api/recipes/{id}/approve/
```
Изменяет статус рецептуры на "approved" и устанавливает дату утверждения.

---

## 🧫 Культуры

### Список культур
```
GET http://127.0.0.1:8000/api/cultures/
```

**Параметры запроса:**
- `search` - поиск по названию и примечаниям
- `status` - фильтр по статусу (active, frozen, discarded)
- `ordering` - сортировка (name, seeding_date, passage_number)
- `page` - номер страницы

**Пример:**
```
GET http://127.0.0.1:8000/api/cultures/?status=active
```

### Создать культуру
```
POST http://127.0.0.1:8000/api/cultures/
Content-Type: application/json

{
    "name": "HEK293",
    "status": "active",
    "passage_number": 5,
    "recipe": 1,
    "responsible": 1,
    "notes": "Клеточная линия эмбриональной почки человека"
}
```

### Детальная информация о культуре
```
GET http://127.0.0.1:8000/api/cultures/{id}/
```

### События культуры
```
GET http://127.0.0.1:8000/api/cultures/{id}/events/
```
Возвращает все события (пассажи, подкормки и т.д.) для конкретной культуры.

---

## 📅 События культур

### Список событий
```
GET http://127.0.0.1:8000/api/culture-events/
```

**Параметры запроса:**
- `search` - поиск по названию культуры и комментарию
- `ordering` - сортировка (date, event_type)
- `page` - номер страницы

### Создать событие
```
POST http://127.0.0.1:8000/api/culture-events/
Content-Type: application/json

{
    "culture": 1,
    "event_type": "passage",
    "comment": "Пассаж 5 -> 6"
}
```

---

## ✅ Задачи

### Список задач
```
GET http://127.0.0.1:8000/api/tasks/
```

**Параметры запроса:**
- `search` - поиск по заголовку и описанию
- `status` - фильтр по статусу (new, in_progress, done, cancelled)
- `priority` - фильтр по приоритету (low, normal, high, urgent)
- `my_tasks` - показать только мои задачи (true)
- `created_by_me` - показать задачи, созданные мной (true)
- `ordering` - сортировка (created_at, deadline, priority, status)
- `page` - номер страницы

**Примеры:**
```
GET http://127.0.0.1:8000/api/tasks/?status=in_progress
GET http://127.0.0.1:8000/api/tasks/?my_tasks=true&ordering=deadline
GET http://127.0.0.1:8000/api/tasks/?priority=urgent
```

### Создать задачу
```
POST http://127.0.0.1:8000/api/tasks/
Content-Type: application/json

{
    "title": "Провести эксперимент",
    "description": "Протестировать новую среду",
    "assignee": 2,
    "status": "new",
    "priority": "high",
    "deadline": "2025-12-01T18:00:00Z"
}
```

### Детальная информация о задаче
```
GET http://127.0.0.1:8000/api/tasks/{id}/
```

### Изменить статус задачи
```
POST http://127.0.0.1:8000/api/tasks/{id}/change_status/
Content-Type: application/json

{
    "status": "done"
}
```

### Просроченные задачи
```
GET http://127.0.0.1:8000/api/tasks/overdue/
```
Возвращает задачи с истекшим deadline и статусом "new" или "in_progress".

---

## 💬 Комментарии к задачам

### Список комментариев
```
GET http://127.0.0.1:8000/api/task-comments/
```

### Создать комментарий
```
POST http://127.0.0.1:8000/api/task-comments/
Content-Type: application/json

{
    "task": 1,
    "text": "Задача выполнена успешно"
}
```

---

## 📢 Объявления

### Список объявлений
```
GET http://127.0.0.1:8000/api/announcements/
```

**Параметры запроса:**
- `search` - поиск по заголовку и тексту
- `ordering` - сортировка (published_at, is_pinned)
- `page` - номер страницы

### Создать объявление
```
POST http://127.0.0.1:8000/api/announcements/
Content-Type: application/json

{
    "title": "Важное объявление",
    "text": "Завтра лаборатория закрыта на инвентаризацию",
    "is_pinned": true
}
```

### Закрепленные объявления
```
GET http://127.0.0.1:8000/api/announcements/pinned/
```

---

## 📆 События календаря

### Список событий
```
GET http://127.0.0.1:8000/api/calendar-events/
```

**Параметры запроса:**
- `search` - поиск по теме, описанию и месту
- `start_date` - показать события после указанной даты
- `end_date` - показать события до указанной даты
- `my_events` - показать только мои события (true)
- `ordering` - сортировка (start_datetime, end_datetime)
- `page` - номер страницы

**Примеры:**
```
GET http://127.0.0.1:8000/api/calendar-events/?start_date=2025-11-01
GET http://127.0.0.1:8000/api/calendar-events/?my_events=true
```

### Создать событие
```
POST http://127.0.0.1:8000/api/calendar-events/
Content-Type: application/json

{
    "subject": "Собрание команды",
    "description": "Обсуждение результатов",
    "start_datetime": "2025-11-22T10:00:00Z",
    "end_datetime": "2025-11-22T11:00:00Z",
    "location": "Конференц-зал",
    "participants": [2, 3]
}
```

---

## 📄 Документы

### Список документов
```
GET http://127.0.0.1:8000/api/documents/
```

**Параметры запроса:**
- `search` - поиск по названию и описанию
- `ordering` - сортировка (name, uploaded_at)
- `page` - номер страницы

### Загрузить документ
```
POST http://127.0.0.1:8000/api/documents/
Content-Type: multipart/form-data

{
    "name": "Инструкция по безопасности",
    "description": "Правила работы в лаборатории",
    "file": <binary_file>
}
```

### Детальная информация о документе
```
GET http://127.0.0.1:8000/api/documents/{id}/
```

---

## 📊 Пагинация

Все списковые endpoints поддерживают пагинацию. По умолчанию возвращается 20 элементов на странице.

**Формат ответа:**
```json
{
    "count": 100,
    "next": "http://127.0.0.1:8000/api/reagents/?page=2",
    "previous": null,
    "results": [...]
}
```

**Параметры:**
- `page` - номер страницы (начиная с 1)

---

## 🔍 Поиск и фильтрация

### Поиск
Большинство endpoints поддерживают поиск через параметр `search`:
```
GET http://127.0.0.1:8000/api/reagents/?search=tris
```

### Сортировка
Используйте параметр `ordering` для сортировки. Добавьте `-` для обратной сортировки:
```
GET http://127.0.0.1:8000/api/reagents/?ordering=-created_at
GET http://127.0.0.1:8000/api/tasks/?ordering=deadline,-priority
```

---

## 🔒 Права доступа

- Все API endpoints требуют аутентификации
- Для модификации данных нужны соответствующие права
- Некоторые действия автоматически привязывают текущего пользователя (создание задач, комментариев и т.д.)

---

## 🧪 Тестирование API

### Через браузер
Откройте в браузере:
```
http://127.0.0.1:8000/api/
```

Войдите через веб-интерфейс:
```
http://127.0.0.1:8000/api-auth/login/
```

### С помощью curl

**Получить список реагентов:**
```bash
curl -X GET http://127.0.0.1:8000/api/reagents/ \
  -u username:password
```

**Создать реагент:**
```bash
curl -X POST http://127.0.0.1:8000/api/reagents/ \
  -u username:password \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Новый реагент",
    "category": "buffer",
    "on_hand": 100,
    "min_threshold": 20
  }'
```

### С помощью Python requests

```python
import requests

# Вход
session = requests.Session()
session.auth = ('username', 'password')

# Получить список реагентов
response = session.get('http://127.0.0.1:8000/api/reagents/')
reagents = response.json()

# Создать реагент
new_reagent = {
    "name": "Test Reagent",
    "category": "enzyme",
    "on_hand": 50,
    "min_threshold": 10
}
response = session.post('http://127.0.0.1:8000/api/reagents/', json=new_reagent)
print(response.json())
```

---

## 📝 Примечания

1. Все даты и времена в формате ISO 8601 (например: `2025-11-22T10:00:00Z`)
2. Файлы загружаются через `multipart/form-data`
3. JSON ответы всегда в кодировке UTF-8
4. Поддерживается как JSON, так и Browsable API интерфейс

---

## ⚠️ Ошибки

API использует стандартные HTTP коды состояния:

- `200 OK` - успешный запрос
- `201 Created` - ресурс успешно создан
- `204 No Content` - ресурс успешно удален
- `400 Bad Request` - ошибка в запросе
- `401 Unauthorized` - требуется аутентификация
- `403 Forbidden` - недостаточно прав
- `404 Not Found` - ресурс не найден
- `500 Internal Server Error` - ошибка сервера

**Формат ошибки:**
```json
{
    "field_name": ["Описание ошибки"]
}
```


